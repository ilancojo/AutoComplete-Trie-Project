# AutoComplete-Trie-Project
Building a console-based autocomplete application using a Trie (prefix tree) data structure. The app will suggest word completions based on user input, similar to how search engines work.
